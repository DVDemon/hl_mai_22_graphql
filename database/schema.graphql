schema {
  query: Query
}

type Query {
  author(id: Int): Author
  allAuthors: [Author]
  search(term: String!): [Author!]!
}

type Author  {
  id: Int
  first_name:String!
  last_name:String!
  email:String!
  title:String!
}